<div class="topbar">
  <div class="filter1">
    <%= gravatar_image_tag(current_identity.email, :alt=> current_identity.email, :class=> 'circle split') %><h2 class="cuadro"><%= current_identity.email %></h2>
  </div>
  <button class="cart">
      <i class="cart__icon fa fa-shopping-cart"></i>
      <span class="text-hidden">Shopping cart</span>
      <span class="cart__count"><%= current_order.total_items %></span>
  </button>
</div>
